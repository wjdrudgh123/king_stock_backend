extends layouts/main

block content
    .form.input
        form(action=routes.input, method="post")
            .input-field
                label(for="issue") 재료
                input(type="text", name="issue", id="issue" placeholder="재료", required)
            .input-field
                label(for="name") 회사
                input(type="text", name="name", id="name" placeholder="회사", required)
            input(type="submit", value="인풋")