// Global
const HOME = "/";

// Admin
const ADMIN = "/admin";
const EDIT = "/edit";
const INPUT = "/input";
const LOGIN = "/login";
const LOGOUT = "/logout";
const MAIN = "/main";
const DELETE = "/del";

const DATA = "/data";
const REALTIME = "/realtime";
const SEARCH = "/search";

const routes = {
  home: HOME,
  admin: ADMIN,
  edit: `${EDIT}`,
  login: LOGIN,
  input: INPUT,
  main: MAIN,
  del: DELETE,
  logout: LOGOUT,
  data: DATA,
  realtime: REALTIME,
  search: SEARCH,
};

export default routes;
